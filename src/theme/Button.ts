import type { ComponentStyleConfig } from '@chakra-ui/theme'

export const Button: ComponentStyleConfig = {
  baseStyle: {
    bgColor: 'white',
    color: 'black',
  },
  sizes: {},
  variants: {},
  defaultProps: {},
}
